name: Apply guided discovery and library flow

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: implement-guided-discovery-library
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply implementation
        shell: bash
        run: |
          set +e
          node scripts/implement-guided-discovery.mjs > guided-discovery-diagnostics.log 2>&1
          STATUS=$?
          cat guided-discovery-diagnostics.log
          exit $STATUS
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: guided-discovery-diagnostics
          path: guided-discovery-diagnostics.log
          if-no-files-found: error
      - run: npm install --no-audit --no-fund
      - run: npm run build
      - name: Commit implementation and remove one-time files
        shell: bash
        run: |
          set -euo pipefail
          rm scripts/implement-guided-discovery.mjs
          rm .github/workflows/apply-guided-discovery.yml
          rm guided-discovery-diagnostics.log
          git config user.name "ReadVerse Engineering"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Implement guided discovery and library saving"
          git push origin HEAD:implement-guided-discovery-library

name: Apply and prove NoTVerse workspace correction

# Disabled after the correction was moved into the established PR proof gate.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Finish NoTVerse 10-for-2 proof on PR #53."

name: Finish mobile swipe proof

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  finish:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/finish-mobile-swipe-proof.mjs
      - run: |
          npm install --no-audit --no-fund
          npm run build
      - name: Commit canonical mobile behaviour and remove patch files
        run: |
          rm scripts/finish-mobile-swipe-proof.mjs
          rm .github/workflows/finish-mobile-swipe-proof.yml
          rm -f package-lock.json
          git config user.name "ReadVerse Reader Recovery"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Finish the physical reader mobile swipe"
          git push origin HEAD:${{ github.head_ref }}

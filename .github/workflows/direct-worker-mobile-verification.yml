name: Direct Worker mobile verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-direct-worker:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm install --no-save playwright
      - run: npx playwright install --with-deps chromium
      - run: node scripts/verify-worker-mobile-brand.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: readverse-direct-worker-mobile-evidence
          path: mobile-deployment-evidence
          if-no-files-found: warn

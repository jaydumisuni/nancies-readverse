# Approved NoTVerse brand correction

This branch corrects the rejected branding treatment without redesigning the product.

Required result:

- Use the exact supplied NoTVerse notebook-and-ghost artwork.
- Browser, setup, sidebar, mobile header and welcome surfaces use that artwork.
- Remove the generated SVG substitute.
- Remove the extra notebook symbol.
- Do not repeat a separate NoTVerse wordmark beside or below artwork that already contains NoTVerse.
- Preserve the exact origin line: `Created for Nancy. Shared with the world.`
- Keep the mobile companion chat input fully visible and hide bottom navigation while chat is open.

The branch may not be merged until the strict build passes and fresh desktop/mobile screenshots are visually inspected.

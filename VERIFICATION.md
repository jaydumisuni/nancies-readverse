# ReadVerse verification baseline

Canonical source only. No hydration or patch chain.

Required live checks:
- Nancy’s / READVERSE mobile brand hierarchy
- no Gogo naming or routes
- upload a real PDF to R2 and D1
- list the uploaded book from D1
- open the stored PDF from the reader endpoint
- import a direct supported file URL
- remove a library item and its stored object
- companion chat remains functional
- desktop, tablet, and mobile layouts match the approved dark neon design
